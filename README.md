# Opentelemetry Collector
This is an experimental opentelemetry collector

## Setup & Use
1. Run `docker-compose build && docker-compose up -d`
2. Connect your application to the otlp endpoint and send through some Signals (logs, traces or metrics) through
3. View the 3 file exporters for each Signal
